import { MusicPlayerPill } from '@/components/examples/music-player-pill'
import musicPlayerPillSource from '@/components/examples/music-player-pill.tsx?raw'
import { Story } from './_components/story'

export const meta = { title: 'Music Player Pill', slug: 'music-player-pill', category: 'Patterns', description: 'A compact now-playing control with artwork, progress, and transport actions.' }

# Music Player Pill

A focused audio player pattern for persistent playback controls without taking over the interface.

<Story title="Now playing" description="Play starts an original browser-synthesized demo loop; the source tab contains the reusable component implementation." code={musicPlayerPillSource}>
  <MusicPlayerPill />
</Story>
