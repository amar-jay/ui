import { CircleAlert } from 'lucide-react'
import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'
import { Story } from './_components/story'

export const meta = { title: 'Alert', slug: 'alert', category: 'Feedback', description: 'A concise callout for important information or status.' }

# Alert

Alerts highlight information that needs attention without requiring an immediate response.

<Story title="Default" description="Use an alert icon and a short, actionable message." code={`import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert'

<Alert>
  <AlertTitle>Heads up</AlertTitle>
  <AlertDescription>Your session expires in 10 minutes.</AlertDescription>
</Alert>`}>
  <Alert className="w-full max-w-lg"><CircleAlert /><AlertTitle>Heads up</AlertTitle><AlertDescription>Your session expires in 10 minutes. Save your work to continue.</AlertDescription></Alert>
</Story>
