import { SettingsDialog } from '@/components/examples/settings-dialog'
import settingsDialogSource from '@/components/examples/settings-dialog.tsx?raw'
import { Story } from './_components/story'

export const meta = { title: 'Settings Dialog', slug: 'settings-dialog', category: 'Patterns', description: 'A spacious settings panel that combines a sidebar, breadcrumb, and focused controls.' }

# Settings Dialog

Use a settings dialog when people need to configure a group of related preferences without leaving their current context. It supports controlled open state and controlled preferences, so applications can persist changes themselves.

<Story title="Sidebar settings" description="Each category has its own controls; on small screens, use the section picker in place of the sidebar." code={settingsDialogSource}>
  <SettingsDialog />
</Story>
